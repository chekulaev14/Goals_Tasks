# Система управления целями и задачами

## 📁 Структура проекта

```
Helper2/
├── goals/                          # Система целей
│   ├── index.html                  # Главная страница
│   ├── app.js                      # Frontend логика
│   └── data/                       # JSON данные
│       ├── dreams.json             # Мечты (большие цели)
│       ├── goals.json              # Среднесрочные цели
│       ├── tasks.json              # Текущие задачи
│       └── current_projects.json   # Активные проекты
├── netlify/functions/
│   └── goals-api.js                # API для чтения/записи JSON
└── shared/components/
    └── menu.html                   # Главное меню (добавлена карточка)
```

## 🔧 Как работает

### Frontend (goals/app.js)
- Загружает данные через API (`/api/dreams`, `/api/goals`, `/api/tasks`)
- Отображает мечты, цели и задачи в веб-интерфейсе
- Сохраняет изменения обратно через API

### Backend (netlify/functions/goals-api.js)
- Читает JSON файлы из `goals/data/`
- Записывает изменения обратно в JSON
- Обрабатывает запросы GET и POST

### Данные (goals/data/*.json)
- **dreams.json** — большие долгосрочные цели (Lexus, квартира, дом)
- **goals.json** — среднесрочные цели (дача, выкуп авто, отдых)
- **tasks.json** — текущие задачи (можно привязывать к целям)

## 🚀 Запуск локально

```bash
cd /Users/petrcekulaev/Desktop/Helper2
netlify dev
```

Откроется `http://localhost:8888/goals/`

## 📤 Как пушить изменения

### Обычный коммит
```bash
cd /Users/petrcekulaev/Desktop/Helper2
git add .
git commit -m "Описание изменений"
git push
```

### Коммит с соавторством Claude
```bash
git add .
git commit -m "$(cat <<'EOF'
Описание изменений

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>
EOF
)"
git push
```

## 🌐 Netlify деплой

После `git push` Netlify **автоматически** деплоит изменения на:
- https://helper.dinamika-cargo.ru

Netlify:
1. Видит новый коммит в GitHub
2. Запускает билд
3. Публикует изменения (~2-3 минуты)

## 📝 Как редактировать данные

### Через веб-интерфейс
Открой `http://localhost:8888/goals/` и редактируй через UI

### Напрямую в JSON
Отредактируй файлы в `goals/data/`, затем обнови страницу

## 🔗 Ссылки

- **Локальная разработка:** http://localhost:8888/goals/
- **Прод:** https://helper.dinamika-cargo.ru/goals/
- **Главное меню:** https://helper.dinamika-cargo.ru/shared/components/menu.html
- **GitHub:** https://github.com/chekulaev14/Helper

## ⚠️ Важно

- **Всегда запускай через `netlify dev`** для локальной работы
- **JSON файлы** — единственный источник данных
- **Не удаляй папку `goals/data/`** — там все данные
- **После изменений в функциях** перезапусти `netlify dev`

## 🐛 Если что-то не работает

1. **Данные не загружаются:**
   - Проверь консоль браузера (F12)
   - Проверь логи `netlify dev` в терминале
   - Убедись, что файлы есть в `goals/data/`

2. **Изменения не сохраняются:**
   - Проверь права на запись в `goals/data/`
   - Проверь логи API в терминале

3. **404 на продакшене:**
   - Подожди 2-3 минуты после пуша
   - Проверь статус деплоя на Netlify

## 📊 Структура данных

### dreams.json
```json
{
  "dreams": [
    {
      "id": 1234567890,
      "title": "Lexus LX 500d",
      "cost": 25000000,
      "description": "...",
      "created_at": "2026-01-12T..."
    }
  ],
  "total_cost": 87000000,
  "last_updated": "2026-01-12T..."
}
```

### goals.json
```json
{
  "goals": [
    {
      "id": 1234567890,
      "title": "Обустроить дачу",
      "cost": 1000000,
      "description": "...",
      "status": "active",
      "created_at": "2026-01-12T..."
    }
  ],
  "total_cost": 3500000,
  "last_updated": "2026-01-12T..."
}
```

### tasks.json
```json
{
  "tasks": [
    {
      "id": 1234567890,
      "title": "Название задачи",
      "goalId": 1234567890,
      "description": "...",
      "status": "active",
      "created_at": "2026-01-12T..."
    }
  ],
  "last_updated": "2026-01-12T..."
}
```
